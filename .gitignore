# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local database — dev.db IS committed (Vercel ships it as read-only content).
# Edit content locally, then commit + push to update the live site.
/prisma/dev.db-journal
/prisma/dev.db-wal
/prisma/dev.db-shm

# portal (moved in)
/config.json
/data/leads.db
/data/leads.db-*
/wa-checker/.wwebjs_auth
/wa-checker/.wwebjs_cache
/wa-checker/wa-results.*
/wa-checker/node_modules
/portal-exports/
